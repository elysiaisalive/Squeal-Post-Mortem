function charAbilitySlowMo() {
	// if ( stats.style == 0 )  {
	// 	drain = false;
	// 	obj_hud.slowmo_active = false;
	// }
	
	// if ( ( stats.style > 0 ) && !drain && input_check_pressed( "key_ability1" ) ) {
	// 	OnActivateSlowMo();
	// 	drain = true;
	// }
		
	// if ( drain ) {
	// 	timeslow_ease_timer = min( timeslow_ease_timer + 1, timeslow_ease_duration );
	// 	stats.style = max( 0, stats.style - 1 );
	// }
	// else {
	// 	timeslow_ease_timer = max( 0, timeslow_ease_timer - 1 );
	// }
		
	// stats.style = clamp( stats.style, 0, stats.style_max );

	// timeslow_ease_scale = ( timeslow_ease_timer / timeslow_ease_duration );
	// timeslow_scale = lerp( 0, 1, timeslow_ease_scale );
	// timescale = lerp( 1, global.slomo_amount, timeslow_scale );
//	obj_control.zoomblur_scale = lerp( 0, global.slomo_amount, timeslow_scale );
}